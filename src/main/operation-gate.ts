// Compatibility export for tests and downstream imports. Application-level
// serialization lives outside the Electron adapter layer.
export { OperationGate } from '../application/operation-gate.js';
